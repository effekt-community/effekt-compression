module main // must be named same as the file!

import args
import src/lib
import stream
import io
import io/error

def main(): Unit = {
    //println("README.md")
    
    huffmanCompressFile("README.md", "README.bin")

    /*val paths: List[String] = commandLineArgs()
    if (paths.size == 0) { 
        println("No file specified, run `cmp [FILENAME]*`")
    } else {
        with on[MissingValue[String]].report
        val file: String = paths.get(0)

    }*/
}